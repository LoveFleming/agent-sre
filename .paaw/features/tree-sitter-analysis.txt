# Project Source Analysis (Tree-sitter)
Parsed 45 of 47 source files
Languages: javascript(29), tsx(14), typescript(4)

## server/agent-loop.mjs [javascript]
  Imports:
    * ← ./llm.mjs
    * ← ./tool-registry.mjs
  Exports:
    function: runAgentLoop
    function: runAgentLoopStream
  Functions:
    function trimMessages(messages, maxChars = DEFAULT_CONTEXT_WINDOW * 4)
    function getCrewTools(crew, allTools)
    async function runAgentLoop({
  crew,
  message,
  history = [],
  model,
  onToolCall,
})
    async function runAgentLoopStream({ crew, message, history = [], model, res, onToolCall })
    arrow send(event, data)
    function buildSystemPrompt(crew)

## server/config.mjs [javascript]
  Imports:
    * ← fs
    * ← path
    * ← url
  Exports:
    variable: ROOT
    variable: config
    function: resolveLLM
    variable: PORT
  Functions:
    function loadProviderConfig()
    function resolveLLM(modelOverride)

## server/config.test.mjs [javascript]
  Imports:
    * ← vitest
    * ← ./config.mjs

## server/conversation.mjs [javascript]
  Imports:
    * ← fs
    * ← path
    * ← url
  Exports:
    function: loadConversation
    function: saveConversation
    function: archiveConversation
    function: listArchives
    function: loadArchive
  Functions:
    function ensureDir(dir)
    function sanitizeId(id)
    function loadConversation(crewId)
    function saveConversation(crewId, messages)
    function archiveConversation(crewId)
    function listArchives(crewId)
    function loadArchive(crewId, sessionId)

## server/conversation.test.mjs [javascript]
  Imports:
    * ← vitest
    * ← ./conversation.mjs
    * ← path
    * ← url
    * ← fs

## server/crew-loader.mjs [javascript]
  Imports:
    * ← fs
    * ← path
    * ← url
  Exports:
    function: loadAllCrews
    function: getCrew
    function: listCrews
  Functions:
    function loadAllCrews()
    function getCrew(id)
    function listCrews()

## server/index.mjs [javascript]
  Imports:
    * ← http
    * ← ./config.mjs
    * ← ./tool-loader.mjs
    * ← ./crew-loader.mjs
    * ← ./routes.mjs
    * ← fs
    * ← path
    * ← url
  Functions:
    async function main()

## server/llm.mjs [javascript]
  Imports:
    * ← ./config.mjs
  Exports:
    function: callLLM
  Functions:
    async function sleep(ms)
    async function callLLM({
  model,
  messages,
  tools,
  maxTokens = 4096,
  maxRetries = 3,
  timeoutMs = 120_000,
  onEvent = null,
})
    function fallbackOverride()
    async function handleStream(resp, onEvent)
    function mergeToolCalls(existing, deltas)

## server/llm.test.mjs [javascript]
  Imports:
    * ← vitest
    * ← ./llm.mjs
  Functions:
    function jsonResponse(body, ok = true, status = 200)
    function streamResponse(chunks)

## server/routes.mjs [javascript]
  Imports:
    * ← ./crew-loader.mjs
    * ← ./agent-loop.mjs
    * ← ./tool-registry.mjs
    * ← ./tool-loader.mjs
    * ← ./conversation.mjs
    * ← ./task-store.mjs
    * ← fs
    * ← path
    * ← url
  Exports:
    function: registerRoutes
  Functions:
    function readBody(req)
    function json(res, code, data)
    function registerRoutes(server)
    arrow onToolCall({ name, result })

## server/routes.test.mjs [javascript]
  Imports:
    * ← vitest
    * ← http
    * ← path
    * ← url
    * ← ./routes.mjs
    * ← ./tool-loader.mjs
  Functions:
    function httpGet(pathname)

## server/task-store.mjs [javascript]
  Imports:
    * ← crypto
  Exports:
    variable: taskStore
    default value: taskStore
  Classes:
    class TaskStore { constructor, list, get, create, update, delete, clear }

## server/task-store.test.mjs [javascript]
  Imports:
    * ← vitest
    * ← ./task-store.mjs
  Functions:
    function makeStore()

## server/tool-engine/provider.mjs [javascript]
  Imports:
    * ← ../llm-utils.mjs
    * ← url
    * ← path
    * ← fs
  Exports:
    class: OpenAICompatibleAdapter
    function: createProviderAdapter
  Functions:
    function _llmLog(entry)
    function stableStringify(value, space)
    function sortKeysDeep(value)
    arrow logStream(line)
    function createProviderAdapter(config)
  Classes:
    class OpenAICompatibleAdapter { constructor, chat }

## server/tool-loader.mjs [javascript]
  Imports:
    * ← fs
    * ← path
    * ← url
    * ← ./tool-registry.mjs
  Exports:
    function: safeResolve
    function: loadAllTools
  Functions:
    function safeResolve(base, ...child)
    async function loadAllTools()
    async arrow wrappedHandler(args, ctx)

## server/tool-loader.test.mjs [javascript]
  Imports:
    * ← vitest
    * ← ./tool-loader.mjs
    * ← path
    * ← url
    * ← fs
    * ← os

## server/tool-registry.mjs [javascript]
  Exports:
    variable: toolRegistry
    default value: toolRegistry
  Classes:
    class ToolRegistry { constructor, register, registerAll, getDefinitions, get, list, execute, clear }

## server/tool-registry.test.mjs [javascript]
  Imports:
    * ← vitest
    * ← ./tool-registry.mjs
  Functions:
    function makeRegistry()
    function sampleEntry(name = "gr")

## tools/docs/handler.mjs [javascript]
  Exports:
    default function: handler
  Functions:
    function parseFrontmatter(content)
    function scanRunbooks()
    function scan(dir)
    function searchRunbooks(keyword, runbooks)
    async function handler(args, ctx)

## tools/grafana/handler.mjs [javascript]
  Imports:
    * ← fs
    * ← path
    * ← url
  Exports:
    default function: handler
  Functions:
    function getConfig()
    function apiBase()
    function authHeaders()
    async function gql(path, options = {})
    function parseTime(t)
    function formatTable(data)
    async function handler(args, ctx)

## tools/k8s/handler.mjs [javascript]
  Exports:
    default function: handler
  Functions:
    function buildBaseArgs()
    function isDestructive(action)
    function runKubectl(args, timeout = 15000)
    function truncateOutput(text, maxLines = 80)
    async function handler(args, ctx)

## tools/loki/handler.mjs [javascript]
  Exports:
    default function: handler
  Functions:
    function getBaseUrl()
    function buildHeaders()
    function formatLogResults(data)
    function formatStatsResults(data)
    async function handler(args, ctx)

## tools/prometheus/handler.mjs [javascript]
  Exports:
    default function: handler
  Functions:
    function getConfigUrl(config)
    function buildHeaders(config)
    async function fetchProm(baseUrl, path, params, config)
    function formatInstantResult(data)
    function formatRangeResult(data)
    function formatAlerts(data)
    async function handler(args, ctx)

## tools/security/handler.mjs [javascript]
  Exports:
    default function: handler
  Functions:
    function runCmd(cmd, timeout = 20000)
    function hasBin(bin)
    function scanRbac(namespace, opts)
    function checkSsl(host, port)
    function scanDeps(projectPath, scanner, severity)
    async function handler(args, ctx)

## tools/shell/handler.mjs [javascript]
  Exports:
    default function: handler
  Functions:
    function isBlocked(cmd)
    function isSafe(cmd)
    function isDestructive(cmd)
    function runCommand(cmd, workdir, timeout)
    function truncateOutput(text, maxLines = 80)
    async function handler(args, ctx)

## tools/tchat/handler.mjs [javascript]
  Imports:
    * ← fs
    * ← path
    * ← url
  Exports:
    default function: handler
  Functions:
    function getConfig()
    function apiBase()
    function checkAuth(chatId)
    async function tgApi(method, params = {})
    function fmtTime(unix)
    function fmtMessage(msg)
    async function handler(args, ctx)

## ui/SREConsole.tsx [tsx]
  Imports:
    * ← ../api
    * ← react
    * ← ../utils
    * ← ../i18n
    * ← ../theme
    * ← ../components/MarkdownText
    * ← ../components/AgentConsole
    * ← ./SREDashboard
  Exports:
    default function: SREConsole
  Functions:
    function SREConsole()
  React Components:
    function: SREConsole

## ui/SREDashboard.tsx [tsx]
  Imports:
    * ← ../api
    * ← react
    * ← ../theme
    * ← ../utils
    * ← ../components/MarkdownText
  Exports:
    default function: SREDashboard
  Functions:
    function ProviderCard({ provider }: { provider: ProviderStatus })
    function AlertCard({ alert }: { alert: Alert })
    function QuickQueryPanel()
    function SREDashboard()
  React Components:
    function: ProviderCard
    function: AlertCard
    function: QuickQueryPanel
    function: SREDashboard

## ui/src/App.tsx [tsx]
  Imports:
    * ← react
    * ← ./components/Sidebar
    * ← ./pages/HomePage
    * ← ./pages/ConsolePage
    * ← ./pages/ToolsPage
    * ← ./pages/TaskManagementPage
    * ← ./pages/Placeholder
    * ← ./types
  Exports:
    default function: App
  Functions:
    function App()
    async arrow check()
  React Components:
    function: App

## ui/src/components/Sidebar.test.tsx [tsx]
  Imports:
    * ← vitest
    * ← @testing-library/react
    * ← @testing-library/user-event
    * ← ./Sidebar
    * ← ../types
  Functions:
    arrow defaultProps()

## ui/src/components/Sidebar.tsx [tsx]
  Imports:
    * ← ../types
    * ← ./StatusBadge
  Exports:
    default function: Sidebar
  Functions:
    function Sidebar({
  items,
  activeId,
  onSelect,
  healthStatus,
}: SidebarProps)
  React Components:
    function: Sidebar

## ui/src/components/StatusBadge.test.tsx [tsx]
  Imports:
    * ← vitest
    * ← @testing-library/react
    * ← ./StatusBadge

## ui/src/components/StatusBadge.tsx [tsx]
  Exports:
    default function: StatusBadge
  Functions:
    function StatusBadge({ status }: StatusBadgeProps)
  React Components:
    function: StatusBadge

## ui/src/main.tsx [tsx]
  Imports:
    * ← react
    * ← react-dom/client
    * ← ./App
    * ← ./index.css

## ui/src/pages/ConsolePage.tsx [tsx]
  Imports:
    * ← react
    * ← react-markdown
    * ← remark-gfm
    * ← ../types
  Exports:
    default function: ConsolePage
  Functions:
    function ConsolePage({
  initialPrompt,
  onPromptConsumed,
}: ConsolePageProps)
  React Components:
    function: ConsolePage

## ui/src/pages/HomePage.test.tsx [tsx]
  Imports:
    * ← vitest
    * ← @testing-library/react
    * ← @testing-library/user-event
    * ← ./HomePage
    * ← ../types
  Functions:
    function mockFetchSuccess()
    function mockFetchFailure()

## ui/src/pages/HomePage.tsx [tsx]
  Imports:
    * ← react
    * ← ../types
    * ← ../types
  Exports:
    default function: HomePage
  Functions:
    function HomePage({ onNavigate, onQuickAction }: HomePageProps)
    function StatCard({
  label,
  value,
  icon,
}: {
  label: string;
  value: string | number;
  icon: string;
})
  React Components:
    function: HomePage
    function: StatCard

## ui/src/pages/Placeholder.tsx [tsx]
  Exports:
    default function: Placeholder
  Functions:
    function Placeholder({ title, phase, icon = "🚧" }: PlaceholderProps)
  React Components:
    function: Placeholder

## ui/src/pages/TaskManagementPage.tsx [tsx]
  Imports:
    * ← react
    * ← ../types
    * ← ../types
  Exports:
    default function: TaskManagementPage
  Functions:
    function TaskManagementPage()
    function EmptyState({ onNewTask }: { onNewTask: () => void })
    function Field({
  label,
  required,
  hint,
  children,
}: {
  label: string;
  required?: boolean;
  hint?: string;
  children: React.ReactNode;
})
    function LinesField({
  label,
  description,
  value,
  onChange,
  placeholder,
}: {
  label: string;
  description?: string;
  value: string;
  onChange: (text: string) => void;
  placeholder?: string;
})
    function ToolPicker({
  tools,
  selected,
  onToggle,
}: {
  tools: ToolEntry[];
  selected: string[];
  onToggle: (toolName: string) => void;
})
  React Components:
    function: TaskManagementPage
    function: EmptyState
    function: Field
    function: LinesField
    function: ToolPicker

## ui/src/pages/ToolsPage.tsx [tsx]
  Imports:
    * ← react
    * ← ../types
    * ← ../types
  Exports:
    default function: ToolsPage
  Functions:
    function ToolsPage()
  React Components:
    function: ToolsPage

## ui/src/test/setup.ts [typescript]
  Imports:
    * ← @testing-library/jest-dom/vitest
    * ← vitest
    * ← @testing-library/react

## ui/src/types.test.ts [typescript]
  Imports:
    * ← vitest
    * ← ./types

## ui/src/types.ts [typescript]
  Exports:
    function: emptyTaskDraft
    function: linesToText
    function: textToLines
    function: getProvider
    function: formatUptime
  Functions:
    function emptyTaskDraft()
    function linesToText(lines: string[])
    function textToLines(text: string)
    function getProvider(source?: string)
    function formatUptime(seconds?: number)

## ui/vite.config.ts [typescript]
  Imports:
    * ← vite
    * ← @vitejs/plugin-react
    * ← path
    * ← url

## vitest.config.mjs [javascript]
  Imports:
    * ← vitest/config
