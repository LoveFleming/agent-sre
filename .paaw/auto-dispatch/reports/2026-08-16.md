# 🎖️ Engineering Manager 報告

**日期：** 2026-08-16
**時間：** 13:07:24
**結果：** ✅ 4 成功 / ❌ 4 失敗 / 8 總計
**模式：** EM 智慧調度

---

## 📊 專案現況

## 專案現況摘要

### Git Status（未提交變更）
```
M .paaw/CHANGELOG.md
 M .paaw/DECISIONS.md
 D .paaw/INSPECTION-DESIGN.md
 D .paaw/PHASE-0-DESIGN.md
 D .paaw/PHASE-1-DESIGN.md
 D .paaw/PLATFORM-ROADMAP.md
 D .paaw/action-log/2026-08-02.json
 M .paaw/agents/_config.json
 M .paaw/agents/coding.architect.json
 M .paaw/agents/coding.developer.json
 M .paaw/agents/coding.doc-writer.json
 M .paaw/agents/coding.em.json
 M .paaw/agents/coding.helpdesk.json
 M .paaw/agents/coding.qa.json
 M .paaw/agents/coding.tester.json
 M .paaw/changes/change-intelligence.json
 M .paaw/code-intelligence/call-graph.json
 M .paaw/code-intelligence/dependency-graph.json
 M .paaw/code-intelligence/file-map.json
 M .paaw/code-intelligence/status-cache.json
 M .paaw/code-intelligence/summary.json
 M .paaw/code-intelligence/symbol-index.json
 M .paaw/code-intelligence/test-code-map.json
 M .paaw/code-intelligence/test-intelligence.json
 M .paaw/coding-memory/actions.jsonl
 M .paaw/coding-memory/conversations/coding.architect/active.json
 D .paaw/coding-memory/conversations/coding.architect/s-2026-08-02T05-44-24.json
 D .paaw/coding-memory/conversations/coding.developer/active.json
 D .paaw/coding-memory/conversations/coding.developer/s-2026-08-01T14-06-39.json
 D .paaw/coding-memory/conversations/coding.developer/s-2026-08-02T03-10-51.json
 D .paaw/coding-memory/conversations/coding.developer/s-2026-08-02T05-50-14.json
 M .paaw/coding-memory/conversations/coding.em/active.json
 D .paaw/coding-memory/conversations/coding.em/s-2026-08-02T10-02-07.json
 D .paaw/coding-memory/conversations/coding.tester/active.json
 M .paaw/coding-memory/dispatch-log.jsonl
 M .paaw/cu-status.json
 M .paaw/em/config.json
 M .paaw/features/FEATURES.json
 M .paaw/features/FILE-FEATURES.json
 M .paaw/features/tree-sitter-analysis.txt
 D .paaw/logs/semgrep-2026-08-01T03-06-42-stdout.json
 D .paaw/logs/semgrep-2026-08-01T03-06-42.sh
 D .paaw/night-shift/config.json
 D .paaw/night-shift/status.json
 M .paaw/project/ARCHITECTURE.md
 M .paaw/project/PROJECT.md
 M .paaw/scan.json
 M .paaw/security/scan-results.json
 D .paaw/sessions/2026-08-02-task.md
 M .paaw/specs/api-contract.md
 M .paaw/specs/error-codes.md
 M .paaw/staged-changes.json
 M .paaw/standards/coding-style.md
 M .paaw/tasks/TASKS.json
 M server/conversation.mjs
 M server/routes.mjs
 M server/tool-loader.mjs
?? .paaw/auto-dispatch/
?? .paaw/deps-cache.json
?? .paaw/gates.json
?? .paaw/logs/semgrep-2026-08-15T11-20-42-stdout.json
?? .paaw/logs/semgrep-2026-08-15T11-20-42.sh
?? .paaw/metrics-cache.json
?? .paaw/sessions/2026-08-15-code-review-task-001serveragent-storemjs-serveragent-storete.md
?? .paaw/sessions/2026-08-15-task-001-git-task-001-commit-qa-review-task-001serveragent-s.md
?? .paaw/sessions/2026-08-15-task-001serveragent-storemjs-file-based-agent-registry-conte.md
?? .paaw/sessions/2026-08-15-task-001serveragent-storemjs-file-based-agent-registry-sa-re.md
?? nul
?? server/config.test.mjs
?? server/conversation.test.mjs
?? server/llm.test.mjs
?? server/routes.test.mjs
?? server/task-store.test.mjs
?? server/tool-registry.test.mjs
```

### Diff Stat（最近變更統計）
```
.paaw/agents/coding.handover.json | 30 ++++++++++++++++++++++++++++++
 .paaw/agents/coding.ops.json      | 30 ++++++++++++++++++++++++++++++
 .paaw/agents/coding.rm.json       | 30 ++++++++++++++++++++++++++++++
 3 files changed, 90 insertions(+)
```

### 最近 commit
```
9de054a chore(.paaw): sync crew snapshots — 沈書桓→Zofia, 林方正→Greta, 趙威霆→Piotr
```

### Action Log（Change 水位 — 最近 20 條 agent 變更紀錄）
[14:06] em/review: TASK-001 agent-store.mjs 完成並 commit（f3e68fc）：SA 設計 → Priya 實作 → QA 審查通過 → server/agent-store.mjs, server/agent-store.test.mjs, agents/README.md [fixed]
[14:06] developer/fix: TASK-001 收尾：三檔案先前僅 staged 未 commit，已用 pathspec commit 單獨提交為 f3e68fc [fixed]
[14:04] qa/review: TASK-001 agent-store.mjs 審查完成：8 項全過（PASS），無 critical/major 問題，獨立重跑測試 63/63 + 全套 214/214 綠 → server/agent-store.mjs, server/agent-store.test.mjs, agents/README.md [fixed]
[13:54] developer/create: TASK-001 agent-store.mjs 實作完成，63+214 測試綠 → server/agent-store.mjs, server/agent-store.test.mjs, agents/README.md [created]
[13:05] architect/decide: 完成 SRE Watchdog Agent Platform 規劃：記錄 ADR-003/ADR-004，開立 15 張 task（TASK-001~015） [adr]

### 專案知識

### PROJECT.md
# agent-sre

> A standalone multi-agent SRE assistant platform that orchestrates LLM-powered agents — equipped with Grafana and chat (tchat) tools via MCP — behind a React web console with multi-tab chat, task management, and tool testing.

> **⚠️ Provenance note:** The automated file-tree scan failed (`find` shell syntax error), and several analysis outputs (Feature Map, Code Intelligence, Test Intelligence, Error Mapping, Security scan, Coding Standards) were not delivered. This document is reconstructed primarily from git history and the Architecture Map / API Contract that were produced. Items marked *(inferred)* are unverified against source. **First task for any reader: re-run the scan with a quoted `find` expression and verify inferred paths.**

## Quick Links
- [Architecture Map](ARCHITECTURE.md) — generated; contains inferred paths requiring verification
- [Feature Map](features/FEATURES.json) — ⚠️ not yet generated
- [API Contract](specs/api-contract.md) — generated; endpoint paths carry confidence ratings (🟢/🟡/🔴)
- [Error Codes](specs/error-codes.md) — ⚠️ not yet generated (Error Mapping run incomplete)
- [Coding Standards](standards/coding-style.md) — ⚠️ not yet generated
- [Code Intelligence](code-intelligence/summary.json) — ⚠️ not yet generated
- [Security Scan](security/scan-results.json) — ⚠️ not yet generated (two security fixes exist in history — see Recent Changes)
- [Knowledge Base](.paaw/) — includes ADR-002 (multi-agent orchestration architecture)

## Tech Stack
- Language: JavaScript (ES Modules, `.mjs`) for server; JSX for UI
- Framework: React + Vite + Tailwind CSS (UI, matched to the parent PAAW stack); custom Node.js HTTP server (no web framework confirmed)
- Protocol: MCP (Model Context Protocol) — both server and client roles
- Database: None confirmed — Task persistence mechanism unverified (likely file or in-memory)
- Runtime: Node.js (server), browser (UI)
- External services: LLM provider APIs (multi-provider, model selector), Gr

### DECISIONS.md
# Technical Decisions

> 記錄架構和技術決策 (ADR format)。AI 在做決策時會自動追加。



### CODING-STANDARDS.md
# Coding Standards

> 本專案的 Coding 規範。AI 在寫碼時必須遵守。

## 通用原則

1. 改完碼一定要 commit + push，不留 uncommitted local change
2. 新字串必須用 t() + 加 locale key（如適用）
3. 永遠處理 IME composition（useRef，不要用 useState）

## 規範子目錄

將各語言/框架的規範放在 `standards/` 子目錄：

- `standards/typescript.md` — TypeScript 規範
- `standards/react.md` — React 規範
- `standards/naming.md` — 命名規範
- `standards/git-commit.md` — Commit message 規範

> 可透過 Coding IDE 的 Standards Editor 編輯，或點「Import」匯入範本。


### CHANGELOG.md
# Changelog

> 由 PAAW AI-Native IDE 自動維護。每次 AI 完成任務後自動追加變更記錄。



### 📝 Open Tasks (15)

**TASK-001** [critical] feature → developer
[P1] agent-store.mjs — file-based Agent Registry 持久化
Pipeline pending: implement, test, qa, docs, commit
依 conversation.mjs 的 file-based 模式，實作 server/agent-store.mjs。Schema：{ id, name, description, context, agentRules, prompt, allowedTools[], schedule (cron expression 或 null 表示僅手動), notifyTarget: { targe

**TASK-002** [high] feature → unassigned
[P1] /api/agents CRUD endpoints
Pipeline pending: implement, review, test, qa, docs, commit
在 server/routes.mjs 加 GET /api/agents（列表）、POST /api/agents（建立，server 產 id）、PUT /api/agents/:id、DELETE /api/agents/:id。錯誤處理沿用現有慣例：400（缺必填欄位：name、prompt）、404（id 不存在）、500（server error），error body { "erro

**TASK-003** [medium] feature → unassigned
[P1] TaskManagementPage 升級為 AgentsPage
Pipeline pending: implement, review, test, qa, docs, commit
升級 ui/src/pages/TaskManagementPage.tsx 成 AgentsPage：欄位擴充 schedule（cron 輸入 + 說明提示）、notifyTarget（targetType select + targetId）、cooldownMinutes、enabled toggle。頁面與 sidebar 選單文字從 Task 改為 Agent。沿用現有 Field/L

**TASK-004** [high] feature → unassigned
[P2] run-store.mjs + /api/runs 查詢 endpoints
Pipeline pending: implement, review, test, qa, docs, commit
server/run-store.mjs：每次執行存 runs/&lt;agentId&gt;/&lt;timestamp&gt;.json。記錄：{ id, agentId, status: "running"|"success"|"failed", startedAt, finishedAt, durationMs, summary（LLM 最終結論摘要）, notified: true/fa

**TASK-005** [high] feature → unassigned
[P2] scheduler.mjs — node-cron 排程引擎
Pipeline pending: implement, review, test, qa, docs, commit
server/scheduler.mjs：用 node-cron（新增 devDependency）。startScheduler() 在 server/index.mjs 啟動時：讀取所有 enabled 且 schedule 非空的 agent 註冊 cron job。到點執行：getAgent → 組裝 crew 物件（prompt/context/agentRules/allowedToo

**TASK-006** [medium] feature → unassigned
[P2] POST /api/agents/:id/run 手動觸發
Pipeline pending: implement, review, test, qa, docs, commit
routes.mjs 加 POST /api/agents/:id/run：立即以 scheduler 同款的執行路徑跑一次（不經 cron 排程、不受 enabled 限制），回 202 + { runId }。供 UI「立即測試」按鈕與驗收 demo 使用。若該 agent 已有 run 進行中，回 409 { error: "agent is already running" }。

**TASK-007** [high] refactor → unassigned
[M] tchat transport 抽層 + 契約 v0（ports &amp; adapters）
Pipeline pending: implement, review, test, qa, docs, commit
依 ADR-004 拆 tools/tchat/handler.mjs：(1) 工具層 tchat_send_message({ targetType: "user"|"channel", targetId, text }) 保持 LLM 介面不變，負責驗證與錯誤格式化；(2) 新增 tools/tchat/client.mjs transport 層實作契約 v0：POST {api_url}/

**TASK-008** [high] chore → unassigned
[M] Grafana mock server（dev fixture）
Pipeline pending: implement, review, test, qa, docs, commit
dev/mocks/grafana-mock.mjs：標準 Node http server（port 3001），實作 grafana handler 用到的 endpoint：GET /api/search、GET /api/dashboards/uid/:uid、POST /api/ds/query、GET /api/prometheus/grafana/api/v1/rules（alert

**TASK-009** [medium] chore → unassigned
[M] tchat mock server（照契約 v0）
Pipeline pending: implement, review, test, qa, docs, commit
dev/mocks/tchat-mock.mjs：port 3002，實作契約 v0 的 POST /api/messages（回 { ok: true, messageId }）。每筆收到的訊息 append 到 dev/mocks/tchat-sent.jsonl（{ ts, targetType, targetId, text }）— 驗證 agent 通知行為就是讀這個檔。POST /__

**TASK-010** [high] feature → unassigned
[P2] Watchdog 範本 agent（seed + 驗收）
Pipeline pending: implement, review, test, qa, docs, commit
建立第一個 watchdog agent（agents/ 目錄 seed 一個範例 JSON + 文件說明）。allowedTools: grafana 系列 + tchat_send_message。context 寫明判斷準則：以 Grafana alerts API 客觀狀態為準（state=firing 才通知，pending 僅觀察並記錄），LLM 負責綜合與措辭，不憑 metric 單

**TASK-011** [medium] feature → unassigned
[P3] datasource-store.mjs + /api/datasources CRUD（key mask）
Pipeline pending: implement, review, test, qa, docs, commit
server/datasource-store.mjs + routes：管理 datasource 設定（例 grafana: { url, token }），存 datasources/*.json。API：GET /api/datasources 回傳時 token 一律 mask 成 ***（永不回明文）、POST/PUT 寫入明文、PUT 若 token 欄為 *** 或缺空則保留原值不

**TASK-012** [low] feature → unassigned
[P3] DatasourcesPage UI
Pipeline pending: implement, review, test, qa, docs, commit
ui/src/pages/ 新增 DatasourcesPage：列表（名稱/type/url/token 顯示 ***）、新增、編輯（token 欄留空或 *** 表示不變更）、刪除（confirm 對話框）。註冊進 sidebar 導覽。沿用現有 Tailwind 慣例。

**TASK-013** [medium] feature → unassigned
[P3] Cooldown / fingerprint 防重複通知
Pipeline pending: implement, review, test, qa, docs, commit
run 結果產生 alert fingerprint（例：sorting 過的 alert names+state 雜湊）。通知前檢查：同 agent 同 fingerprint 若在 cooldownMinutes（agent 可覆寫，預設 30）內已通知過，跳過 tchat 發送並在 run 記 notified: false, skippedByCooldown: true。fingerpr

**TASK-014** [medium] feature → unassigned
[P3] RunsPage UI — 執行歷史檢視
Pipeline pending: implement, review, test, qa, docs, commit
ui/src/pages/ 新增 RunsPage：GET /api/runs 列表（時間、agent 名、status badge、耗時、notified 欄）、按 agent 過濾、點單筆展開 GET /api/runs/:id 完整記錄（summary、tool call 摘要、notifyError、error）。StatusBadge 元件可沿用/擴充現有 ui/src/componen

**TASK-015** [high] feature → unassigned
[X] API auth — X-API-Token（T8 之前必須完成）
Pipeline pending: implement, review, test, qa, docs, commit
⚠️ 前置於 T8（datasource CRUD 管理 secrets）。最小可行方案：所有 /api/* 加 X-API-Token header 檢查（值從 env AGENT_SRE_API_TOKEN 讀），用 crypto.timingSafeEqual 常數時間比對，失敗回 401 { error }。UI 端把 token 存 localStorage 並在 fetch wrapp

### 🔒 Security Findings (36)
36 findings (5 files affected). Top: server/conversation.mjs (30), server/routes.mjs (2), server/tool-loader.mjs (2), server/llm.mjs (1), server/tool-engine/provider.mjs (1)

**Top affected files:**
- server/conversation.mjs (30 findings, WARNING): Detected that function argument `dir` has entered the fs module. An attacker could potentially contr
- server/routes.mjs (2 findings, WARNING): Detected that function argument `req` has entered the fs module. An attacker could potentially contr
- server/tool-loader.mjs (2 findings, WARNING): Detected possible user input going into a `path.join` or `path.resolve` function. This could possibl
- server/llm.mjs (1 findings, WARNING): JSON stringify does not produce a stable key ordering, and should not be relied on for producing obj
- server/tool-engine/provider.mjs (1 findings, WARNING): JSON stringify does not produce a stable key ordering, and should not be relied on for producing obj



---

## 📋 工作清單

### 1. ✅ [medium] developer — 工作區清理與未提交變更 commit（effort: S）：目前有未提交的 server/conversation.mjs、server/routes.mjs、server/tool-loader.mjs 修改，加上 6 個 untracked 測試檔（server/config.test.mjs, conversation.test.mjs, llm.test.mjs, routes.test.mjs, task-store.test.mjs, tool-registry.test.mjs）。先跑 npm test 確認全套綠燈，然後：(1) 將 server 程式碼修改 + 對應測試檔整理成一個 commit；(2) .paaw/ 中繼資料變更另開一個 chore(.paaw) commit；(3) 刪除根目錄垃圾檔 nul（Windows shell 重定向產物）並加入 .gitignore。注意 server/agent-store.mjs 相關已完成並提交於 f3e68fc，不要重複。只 commit 不 push（9de054a 亦待人工 push）。
> CODING-STANDARDS 要求不留 uncommitted local change，且先清乾淨工作區避免後續 task 的 commit 混入無關變更

**結果：**
```
清理完成。

**執行結果：**

| 步驟 | 結果 |
|---|---|
| `npm test` | ✅ 8 檔 214/214 全綠 |
| Commit 1 | `c7e19b4` `fix(server): harden path traversal guards and add unit tests`（3 程式碼 + 6 測試檔） |
| Commit 2 | `a4a3517` `chore(.paaw): sync metadata after workspace cleanup`（64 檔） |
| Commit 3 | `a97ffa9` `chore: ignore nul file`（.gitignore 補行 — 第一次 add 時漏掉了，單獨補上） |
| nul 垃圾檔 | 已刪除（本來就未被追蹤） |
| agent-store.mjs | 未動（已在 f3e68fc） |
| 工作樹 | 完全乾淨 |

`.paaw/` commit 有多刪了幾個過時檔（night-shift/、PHASE-0/1-DESIGN 等）和新增 session 紀錄，都是工具自動產生的中繼資料同步，屬預期。

📋 **Work Summary**
├─ 改了什麼：
│  - `server/conversation.mjs` — sanitizeId() 驗證 crewId/sessionId 防路徑逃逸
│  - `server/routes.mjs` — 靜態檔服務改用 safeResolve
│  - `server/tool-loader.mjs` — safeResolve 強化
│  - 6 個新增測試檔（config/conversation/llm/routes/task-store/tool-registry）
│  - `.gitig
```

### 2. ✅ [high] developer — 實作 TASK-002：/api/agents CRUD endpoints（effort: M）。在 server/routes.mjs 新增：GET /api/agents（列表）、POST /api/agents（建立，server 端產 id）、PUT /api/agents/:id、DELETE /api/agents/:id。資料層使用已完成的 server/agent-store.mjs（commit f3e68fc），schema 為 { id, name, description, context, agentRules, prompt, allowedTools[], schedule（cron 或 null）, notifyTarget, cooldownMinutes, enabled }。錯誤處理沿用 routes.mjs 現有慣例：400（缺必填欄位 name、prompt）、404（id 不存在）、500，error body { "error": ... }。在 server/routes.test.mjs 補 CRUD 的 200/400/404 測試案例。完整規格見 .paaw/tasks/TASKS.json 的 TASK-002。完成後 commit。
> TASK-002 是 P1 pipeline pending，且其依賴的 agent-store.mjs（TASK-001）已完成並通過 QA，是解鎖後續 UI 與 scheduler 的關鍵路徑

**結果：**
```
TASK-002 完成。實作內容在進入本次工作時已存在於工作樹（前一 session 寫完未 commit），本次完成了驗證與收尾 commit。

📋 **Work Summary**
├─ 改了什麼：
│  - `server/routes.mjs` — 新增 4 條 `/api/agents` routes：GET 列表（200 `{agents:[...]}`）、POST 建立（201，id 由 `saveAgent` 產 UUID）、PUT/DELETE `/api/agents/:id`（200/404）；錯誤慣例 400（缺 `name`/`prompt`、非法 body/id、schema 違規）/ 404（id 不存在）/ `{error}` body。另含兩個規格要求的附帶項目：`/api/tasks` 五個 endpoints 回應加 `deprecated: true`（過渡期並存）、`setSchedulerNotifier()` callback hook（scheduler 尚不存在，僅埋 call site，notifier throw 不影響 HTTP 回應）
│  - `server/routes.test.mjs` — 12 → 41 cases：CRUD 200/201/400/404、缺欄位訊息精確匹配、scheduler hook 三種事件與錯誤隔離、deprecated 標記；用 `SRE_AGENTS_DIR` 指向 tmpdir 隔離，不污染版控的 `agents/` 目錄
│  - `.paaw/api-samples.json` — 5 個 `/api/agents` endpoints 完整 request/response samples（供 Tester/QA 衍生 E2E）
├─ 為什麼改：TASK-002 規
```

### 3. ✅ [high] developer — 實作 TASK-015：API auth X-API-Token（effort: S）。在 server/routes.mjs（或抽出 middleware）為所有 /api/* 動態 endpoints 加 X-API-Token request header 檢查：值從 env AGENT_SRE_API_TOKEN 讀取，用 crypto.timingSafeEqual 做常數時間比對（先比長度並處理長度不符），失敗回 401 { "error": "unauthorized" }。env 未設定 AGENT_SRE_API_TOKEN 時放行並 log 警告（dev 便利性）。靜態檔案與健康檢查 endpoint 不受影響。同步更新 server/routes.test.mjs：既有 /api 測試補上合法 token header，並新增 401（無 token / 錯 token）與無 env 放行的測試。完成後 commit。此 task 排在 TASK-002 之後執行，記得 /api/agents 測試也要帶 token。
> TASK-015 標注為 datasource secrets 管理（T8/TASK-011）的前置必須項，且愈晚加 auth 要改的測試愈多

**結果：**
```
## 📋 Work Summary

**├─ 改了什麼**
| 檔案 | 內容 |
|---|---|
| `server/routes.mjs` | `safeTokenEqual()`（Buffer + 長度預檢 + `crypto.timingSafeEqual`）+ `checkApiToken()` gate；CORS allow-header 加 `X-API-Token` |
| `server/index.mjs` | 啟動時 env 未設 → 醒目 dev-mode 警告 |
| `server/routes.test.mjs` | 41→50 cases：401 無 token / 長度不符 / 同長度不符、health+靜態豁免、dev-mode 放行 |
| `.paaw/api-samples.json` | 7 個 endpoint sample 補 `X-API-Token` header |
| `.paaw/*` | changelog + task metadata |

**├─ 為什麼改**
T8（datasource CRUD）起 API 將具備讀寫 secret 能力，任何非 localhost 暴露都必須先有 auth。TASK-015 是 T8 的前置。

**├─ 特殊情況說明**
進場時工作樹已有一份上個 session 中斷的完整實作。我沒有重寫，而是做了**完整驗證**：讀全部 diff 確認邏輯、跑全套測試 250/250 綠、確認 api-samples 同步，然後 commit 為 `060f0b2`，再將殘留的 `.paaw` metadata 收尾為 `dca65ae` + `e9333a9`。工作樹現在乾淨。

**├─ 怎麼測試**
1. `npx vitest run` → 250/250 綠
2.
```

### 4. ❌ [high] developer — 實作 TASK-004：run-store.mjs + /api/runs 查詢 endpoints（effort: M）。新增 server/run-store.mjs：每次 agent 執行存 runs/<agentId>/<timestamp>.json，記錄 { id, agentId, status: "running"|"success"|"failed", startedAt, finishedAt, durationMs, summary（LLM 最終結論摘要）, notified: true/false, toolCalls 摘要, error }。實作時參考 server/agent-store.mjs 的 file-based store 模式。在 server/routes.mjs 加 GET /api/runs（列表，支援 ?agentId= 過濾，回列表摘要）與 GET /api/runs/:id（完整記錄，404 若不存在）。新增 server/run-store.test.mjs，測試風格參考 server/task-store.test.mjs（temp dir 隔離）。完成後 commit。注意：若 /api/* 已加 X-API-Token（TASK-015），測試需帶 token header 或確保測試環境未設 env。
> TASK-004 是 P2 pipeline pending，run 記錄是 scheduler（TASK-005）與手動觸發（TASK-006）的共同依賴，需先完成

**錯誤：** fetch failed

### 5. ❌ [high] developer — 實作 TASK-005：scheduler.mjs node-cron 排程引擎（effort: M）。新增 server/scheduler.mjs：安裝 node-cron dependency（執行期需要，放 dependencies）。匯出 startScheduler()，在 server/index.mjs 啟動時呼叫：讀取所有 enabled=true 且 schedule 非空的 agent，為每個註冊 cron job。到點執行流程：agent-store getAgent → 組裝 crew 物件（prompt/context/agentRules/allowedTools）→ 走現有 agent 執行路徑（參考 server/conversation.mjs 的 agent 執行與 tools/tchat、tools/grafana handler 接線）→ 用已完成的 server/run-store.mjs 記錄 run（status running → success/failed）→ 依 notifyTarget 發通知；tchat transport（TASK-007）尚未完成前，通知步驟先 log 並在 run 記 notified: false。同一 agent 執行中不重入。補最少單元測試（cron parse 註冊邏輯可用 mock timer）。完成後 commit。前置：TASK-004 的 run-store.mjs 需已存在於 repo。
> TASK-005 是 P2 pipeline pending、Watchdog 平台核心引擎，且依賴的 run-store（TASK-004）已在本批規劃中先行完成

**錯誤：** fetch failed

### 6. ❌ [medium] developer — 實作 TASK-006：POST /api/agents/:id/run 手動觸發（effort: S）。在 server/routes.mjs 加 POST /api/agents/:id/run：立即以 scheduler.mjs 同款執行路徑跑一次（不經 cron 排程、不受 enabled 限制），非同步執行並立即回 202 + { runId }。若該 agent 已有 run 進行中，回 409 { "error": "agent is already running" }。agent 不存在回 404。在 server/routes.test.mjs 補 202/404/409 測試（執行主體可 stub）。完成後 commit。前置：server/scheduler.mjs 與 server/run-store.mjs（TASK-004/005）需已存在於 repo；若 /api/* 已加 X-API-Token，測試帶 token。
> TASK-006 pipeline pending，是 UI「立即測試」按鈕與驗收 demo 的入口，複用 TASK-005 已建好的執行路徑所以排在後面

**錯誤：** fetch failed

### 7. ❌ [medium] developer — 實作 TASK-003：TaskManagementPage 升級為 AgentsPage（effort: M）。升級 ui/src/pages/TaskManagementPage.tsx 成 AgentsPage：欄位擴充 schedule（cron 輸入 + 說明提示文字）、notifyTarget（targetType select + targetId 輸入）、cooldownMinutes（數字輸入，預設 30）、enabled toggle。頁面與 sidebar 選單文字從 Task 改為 Agent。對接後端 GET/POST/PUT/DELETE /api/agents（後端由 TASK-002 完成，error body 為 { "error": ... }，400/404 需在 UI 呈現錯誤訊息）。沿用現有 Field/表單元件與 Tailwind 慣例，IME composition 輸入用 useRef 處理（見 .paaw/standards/coding-style.md）。完成後 commit。
> TASK-003 pipeline pending，讓已完成 agent-store + CRUD API 的資料可在 UI 管理形成閉環，排在 API（TASK-002）之後

**錯誤：** fetch failed

### 8. ✅ [medium] developer — 實作 TASK-008：Grafana mock server dev fixture（effort: M）。新增 dev/mocks/grafana-mock.mjs：標準 Node http server 監聽 port 3001，實作 tools/grafana handler 用到的 endpoints：GET /api/search（回 dashboard 清單 fixture）、GET /api/dashboards/uid/:uid（回單一 dashboard JSON）、POST /api/ds/query（回固定 panel query 結果）、GET /api/prometheus/grafana/api/v1/rules（回 alert rules，含 state 為 firing 與 pending 的各至少一筆，供 watchdog 判斷邏輯驗證）。先讀 server/tools/（或實際 grafana tool 路徑，可用 grep 找 '/api/ds/query'）確認實際呼叫的 endpoint 與 response 形狀再寫 fixture。在 package.json 加 "mock:grafana": "node dev/mocks/grafana-mock.mjs" script。完成後 commit。此為獨立 dev fixture，不影響正式程式碼。
> TASK-008 pipeline pending（high），是 scheduler 與 watchdog agent（TASK-010）驗收的必要基礎建設，且完全獨立可並行

**結果：**
```
TASK-008 已完成（由平行 session 實作、commit `4a13b24`），本 session 做完整驗證通過。

## 驗證結果

**端點與 handler 對齊**（逐行比對 `tools/grafana/handler.mjs` 實際讀取的欄位）：
- `GET /api/search` → 3 個 dashboard，含 `type: "dash-db"`、`folderTitle`、`tags`（handler 的 `list_dashboards`/`search` 用）
- `GET /api/dashboards/uid/:uid` → `dashboard.panels[].targets[].expr` 完整（`query_panel` 兩步查詢可用），未知 uid 回 404
- `POST /api/ds/query` → columnar frame（`schema.fields` + `data.values`），`status: "success"`（`formatTable` 分支可解析）
- `GET /api/prometheus/grafana/api/v1/rules` → firing 情境含 **HighCPUUsage=firing、MemoryPressure=pending**、DiskAlmostFull=ok，`alerts[]` 非空、`type: "alerting"`（watchdog 判斷邏輯可驗證）
- 另含 `/api/datasources`、`/api/health`、legacy `/api/alerts`、datasource proxy `query_range`

**E2E（真實 handler 打 mock）**：6 個 grafana tool 全部正常輸出，`list_a
```

---

*由 PAAW Engineering Manager 自動產生*
