/**
 * App — SRE Agentic Monitoring Workspace.
 *
 * The monitor workspace IS the app: left monitor menu, outer tab sheet,
 * agent chat. No other pages.
 */
import MonitorWorkspace from "./pages/MonitorWorkspace";

export default function App() {
  return <MonitorWorkspace />;
}
